# Petrochemical Data Platform 🏭

[![Go Version](https://img.shields.io/badge/Go-1.24-blue.svg)](https://golang.org)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Docker](https://img.shields.io/badge/Docker-ready-brightgreen.svg)](docker-compose.yml)

Современная платформа для мониторинга и анализа данных нефтехимического производства в России. Система обеспечивает сбор, хранение и визуализацию телеметрических данных от промышленных датчиков в режиме реального времени.

## 🎯 Основные возможности

- **Реальное время**: Мониторинг производственных объемов с частотой обновления 1 секунда
- **9 крупнейших компаний**: СИБУР, Нижнекамскнефтехим, Ангарская НХК, ЗапСибНефтехим и др.
- **19 датчиков**: Мониторинг различных производственных линий и процессов
- **Временные ряды**: Хранение и анализ исторических данных в ClickHouse
- **REST API**: Полнофункциональный API для интеграции
- **Веб-интерфейс**: Интерактивные графики и аналитика

## 📊 Архитектура

```
┌─────────────────┐     ┌──────────────┐     ┌─────────────┐
│   Simulator     │────▶│  ClickHouse  │◀────│  API Server │
│  (генерация)    │     │  (телеметрия)│     │   (REST)    │
└─────────────────┘     └──────────────┘     └──────┬──────┘
                                                     │
┌─────────────────┐     ┌──────────────┐           │
│   PostgreSQL    │◀────│    Redis     │           │
│   (активы)      │     │    (кеш)     │           ▼
└─────────────────┘     └──────────────┘     ┌─────────────┐
                                              │  Frontend   │
┌─────────────────┐                          │   (Web UI)  │
│   MQTT Broker   │                          └─────────────┘
│  (сообщения)    │
└─────────────────┘
```

### Компоненты системы

- **Data Acquisition**: MQTT, REST API, OPC UA, TCP/UDP
- **Storage Layer**: PostgreSQL (метаданные), ClickHouse (временные ряды), Redis (кеширование)
- **Business Logic**: REST API на Gin, бизнес-логика на Go
- **Simulation**: Генератор реалистичных данных для 19 датчиков
- **Frontend**: Интерактивный веб-интерфейс с графиками Recharts

## 🚀 Быстрый старт

### Требования

- Docker & Docker Compose
- Go 1.24+ (для локальной разработки)
- 4GB RAM минимум

### Установка и запуск

1. **Клонировать репозиторий**
```bash
git clone https://github.com/Study-group-8/Petrochemical-Data-Platform.git
cd Petrochemical-Data-Platform
```

2. **Запустить все сервисы**
```bash
docker compose up -d
```

3. **Запустить фронтенд**
```bash
cd frontend
python3 -m http.server 3000
```

4. **Открыть в браузере**
- Фронтенд: http://localhost:3000
- API: http://localhost:8080
- ClickHouse: http://localhost:8124

### Проверка работоспособности

```bash
# Проверить статус сервисов
docker compose ps

# Проверить API
curl http://localhost:8080/api/v1/assets

# Проверить телеметрию
curl http://localhost:8080/api/v1/telemetry/SIBUR_TOBOLSK_PP_001

# Проверить данные в ClickHouse
curl "http://localhost:8124/?query=SELECT%20count()%20FROM%20petrochemical.telemetry"
```

## 📁 Структура проекта

```
Petrochemical-Data-Platform/
├── cmd/
│   ├── api/              # REST API сервер
│   ├── parser/           # ETL процессы
│   └── simulator/        # Генератор данных
├── internal/
│   ├── config/           # Конфигурация (Viper)
│   ├── domain/           # Бизнес-модели
│   ├── handler/          # HTTP обработчики
│   ├── service/          # Бизнес-логика
│   ├── repository/       # Слой данных
│   │   ├── postgres.go   # PostgreSQL
│   │   ├── redis.go      # Redis
│   │   └── clickhouse.go # ClickHouse
│   └── pkg/
│       ├── parser/       # Парсеры данных
│       └── simulator/    # Логика симуляции
├── frontend/
│   └── index.html        # Веб-интерфейс
├── configs/
│   └── config.yaml       # Конфигурация
├── docker/
│   ├── clickhouse-init.sql/  # Инициализация БД
│   └── init.sql          # Схема PostgreSQL
├── docker-compose.yml    # Оркестрация
└── README.md
```

## 🔧 Конфигурация

### Переменные окружения

Основные настройки в `configs/config.yaml`:

```yaml
server:
  port: "8080"
  
database:
  postgresql:
    host: "postgres"
    port: "5432"
    user: "petrochemical"
    password: "petrochemical"
    dbname: "petrochemical"
    
  clickhouse:
    host: "clickhouse"
    port: "9000"
    database: "petrochemical"
    
redis:
  host: "redis"
  port: "6379"
  
mqtt:
  broker: "tcp://mqtt:1883"
```

### Порты сервисов

| Сервис      | Порт  | Описание                    |
|-------------|-------|-----------------------------|
| API         | 8080  | REST API                    |
| Frontend    | 3000  | Веб-интерфейс               |
| PostgreSQL  | 5433  | База метаданных             |
| ClickHouse  | 9001  | Native протокол             |
| ClickHouse  | 8124  | HTTP интерфейс              |
| Redis       | 6380  | Кеш                         |
| MQTT        | 1884  | MQTT брокер                 |

## 📡 API Endpoints

### Assets (Активы)

```bash
# Получить список всех активов
GET /api/v1/assets

# Получить актив по ID
GET /api/v1/assets/{id}

# Создать новый актив
POST /api/v1/assets
```

### Telemetry (Телеметрия)

```bash
# Получить последние данные датчика
GET /api/v1/telemetry/{sensor_id}

# Пример ответа:
{
  "sensor_id": "SIBUR_TOBOLSK_PP_001",
  "data": [
    {
      "sensor_id": "SIBUR_TOBOLSK_PP_001",
      "value": 952.51,
      "unit": "тонны",
      "timestamp": "2025-11-13T19:52:00Z",
      "quality": 192
    }
  ]
}
```

### Control (Управление)

```bash
# Отправить управляющую команду
POST /api/v1/control
```

## 🏭 Датчики и компании

### Крупнейшие компании

1. **СИБУР Холдинг** (Тобольск)
   - `SIBUR_TOBOLSK_PP_001` - Линия полипропилена
   - `SIBUR_TOBOLSK_PE_001` - Линия полиэтилена
   - `SIBUR_TOBOLSK_MTBE_001` - Производство МТБЭ

2. **Нижнекамскнефтехим** (Нижнекамск)
   - `NIZHNEKAMSKNEFTEKHIM_RUBBER_001` - Реактор каучука
   - `NIZHNEKAMSKNEFTEKHIM_PE_001` - Колонна полиэтилена

3. **Ангарская НХК** (Ангарск)
   - `ANGARSK_NHK_ROSNEFT_POLYMER_001` - Резервуар полимеров
   - `ANGARSK_NHK_ROSNEFT_AROMATICS_001` - Линия ароматики

4. **ЗапСибНефтехим** (Тобольск)
   - `ZAPSIBNEFTEKHIM_PP_001` - Реактор полипропилена
   - `ZAPSIBNEFTEKHIM_PE_001` - Линия полиэтилена

...и 5 других компаний с 10 дополнительными датчиками

### Параметры мониторинга

- **Диапазон значений**: 200-1200 тонн/час
- **Частота обновления**: 1 секунда
- **Качество данных**: 192 (хорошее), 0 (сбой)
- **Единицы измерения**: Тонны (производственные объемы)

## 🐳 Docker

### Сборка образов

```bash
# Собрать все сервисы
docker compose build

# Собрать конкретный сервис
docker compose build api
docker compose build simulator
```

### Управление контейнерами

```bash
# Запустить все сервисы
docker compose up -d

# Остановить все сервисы
docker compose down

# Просмотр логов
docker compose logs -f api
docker compose logs -f simulator

# Перезапуск сервиса
docker compose restart api
```

### Очистка данных

```bash
# Остановить и удалить все контейнеры и volumes
docker compose down -v

# Очистка образов
docker compose down --rmi all
```

## 💻 Локальная разработка

### Установка зависимостей

```bash
go mod download
```

### Запуск сервисов по отдельности

```bash
# API сервер
go run cmd/api/main.go

# Симулятор
go run cmd/simulator/main.go

# Парсер (если нужен)
go run cmd/parser/main.go
```

### Тестирование

```bash
# Запуск всех тестов
go test ./...

# Тесты с покрытием
go test -cover ./...

# Запуск конкретного пакета
go test ./internal/service/...
```

## 📊 Использование веб-интерфейса

1. Откройте http://localhost:3000
2. Выберите компанию из списка слева
3. Нажмите "Анализировать данные"
4. Просмотрите графики производственных объемов
5. Экспортируйте данные в CSV при необходимости

### Возможности UI

- 🔍 Поиск по компаниям и продуктам
- 📈 Интерактивные графики (цены и объемы)
- 🎯 Фильтрация по размеру и типу компании
- 📥 Экспорт данных в CSV
- ⚡ Реальные данные из ClickHouse

## 🛠 Технологии

### Backend
- **Go 1.24**: Основной язык программирования
- **Gin**: Веб-фреймворк для REST API
- **pgx/v5**: PostgreSQL драйвер
- **ClickHouse**: База данных для временных рядов
- **Redis**: In-memory кеширование
- **Viper**: Управление конфигурацией
- **Zap**: Структурированное логирование

### Frontend
- **HTML5/CSS3**: Современный UI
- **TailwindCSS**: Utility-first CSS
- **Recharts**: Библиотека графиков
- **Lucide Icons**: Иконки

### Infrastructure
- **Docker**: Контейнеризация
- **Docker Compose**: Оркестрация сервисов
- **MQTT (Mosquitto)**: Брокер сообщений

## 📈 Производительность

- **Пропускная способность**: 10,000+ записей/сек
- **Задержка API**: < 50ms (p95)
- **Хранение данных**: Автоматическая ротация > 30 дней
- **Симуляция**: 19 датчиков × 1 запись/сек = 1,641,600 записей/день

## 🔐 Безопасность

### Защита экспорта данных

Функция экспорта данных в CSV защищена паролем администратора. При попытке экспорта появляется модальное окно с запросом пароля.

**Пароль администратора:** хранится в переменной окружения `ADMIN_EXPORT_PASSWORD` в файле `.env` (не включён в репозиторий)

#### Настройка пароля администратора

1. Создайте файл `.env` в корне проекта:
   ```bash
   ADMIN_EXPORT_PASSWORD=YourSecurePassword123!
   ```

2. Убедитесь, что `.env` добавлен в `.gitignore`

3. Перезапустите API сервер:
   ```bash
   docker compose restart api
   ```

#### Проверка пароля

Пароль проверяется на сервере через API endpoint `/api/v1/auth/verify-export-password` с использованием:
- Безопасного сравнения (constant-time comparison) для защиты от timing attacks
- POST запроса с JSON payload
- Переменной окружения для хранения пароля

#### Рекомендации

- ⚠️ Используйте сложный пароль (буквы, цифры, спецсимволы)
- 🔄 Периодически меняйте пароль в `.env`
- 📝 Храните пароль в надёжном месте (менеджер паролей)
- 🚫 Не коммитьте файл `.env` в публичный репозиторий
- 🔐 Используйте разные пароли для dev/staging/production окружений








