# 🚀 Настройка GitHub Pages

## Шаги для активации

### 1. Перейдите в Settings репозитория

В GitHub откройте: **Settings** → **Pages**

### 2. Настройте Source

- **Source**: Выберите `GitHub Actions`
- Нажмите **Save**

### 3. Активируйте workflow

Workflow уже создан в `.github/workflows/deploy-pages.yml`

### 4. Запустите деплой

Вариант A (автоматически):
```bash
git add .
git commit -m "Enable GitHub Pages"
git push origin main
```

Вариант B (вручную):
- Перейдите в **Actions** → **Deploy to GitHub Pages**
- Нажмите **Run workflow**
- Выберите ветку `main`
- Нажмите **Run workflow**

### 5. Проверьте деплой

После завершения workflow (1-2 минуты):
- Вернитесь в **Settings** → **Pages**
- Увидите ссылку: `https://study-group-8.github.io/Petrochemical-Data-Platform/`
- Откройте её в браузере

## ✅ Готово!

Теперь при каждом push в `main` фронтенд автоматически обновится на GitHub Pages.

## 📝 Custom Domain (опционально)

Если хотите использовать свой домен:
1. **Settings** → **Pages** → **Custom domain**
2. Введите ваш домен (например, `petrodata.example.com`)
3. Настройте DNS записи у вашего регистратора

## 🔧 Troubleshooting

**Деплой не запускается?**
- Проверьте, что выбран Source: "GitHub Actions" (не "Deploy from branch")

**404 ошибка?**
- Подождите 1-2 минуты после первого деплоя
- Очистите кеш браузера (Ctrl+Shift+R)

**Workflow падает с ошибкой?**
- Проверьте логи в **Actions**
- Убедитесь, что файлы в папке `/frontend` корректны
