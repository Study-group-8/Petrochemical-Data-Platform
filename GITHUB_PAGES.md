# 🌐 GitHub Pages Deployment

Фронтенд платформы автоматически развёртывается на GitHub Pages при каждом push в ветку `main`.

## 🔗 Ссылка на демо

**Live Demo:** https://study-group-8.github.io/Petrochemical-Data-Platform/

## ⚙️ Настройка GitHub Pages

### 1. Включить GitHub Pages в репозитории

1. Перейдите в **Settings** → **Pages**
2. В разделе **Source** выберите **GitHub Actions**
3. Сохраните настройки

### 2. Автоматический деплой

После настройки каждый push в `main` автоматически:
- Запускает GitHub Actions workflow
- Собирает frontend из папки `/frontend`
- Публикует на GitHub Pages

### 3. Проверка деплоя

Откройте вкладку **Actions** в репозитории, чтобы увидеть статус деплоя.

## 🎯 Работа с локальным API

По умолчанию фронтенд на GitHub Pages работает с **mock данными** (синтетическая генерация).

### Подключение к локальному API

Если вы хотите использовать реальные данные из локального API:

1. Запустите локальный API сервер:
   ```bash
   docker compose up api clickhouse postgres redis
   ```

2. API будет доступен на `http://localhost:8080`

3. В браузере разрешите CORS (для тестирования):
   - Установите расширение типа "CORS Unblock"
   - Или используйте Chrome с флагом `--disable-web-security`

### Настройка CORS на сервере

В файле `internal/handler/routes.go` уже настроен CORS для локальной разработки:

```go
config := cors.DefaultConfig()
config.AllowOrigins = []string{"http://localhost:3000", "https://study-group-8.github.io"}
config.AllowMethods = []string{"GET", "POST", "PUT", "DELETE", "OPTIONS"}
```

## 📱 Возможности демо

На GitHub Pages доступны все функции:
- ✅ Просмотр 23 компаний
- ✅ Выбор продуктов
- ✅ 3 типа графиков (цена, объем, выручка)
- ✅ Выбор временного периода (6 мес - 10 лет)
- ✅ Календарь произвольного периода
- ✅ Поиск и фильтрация
- ✅ Статистика
- ⚠️ Экспорт CSV (требует API)

## 🛠 Ручной деплой

Чтобы запустить деплой вручную:

1. Перейдите в **Actions** → **Deploy to GitHub Pages**
2. Нажмите **Run workflow**
3. Выберите ветку `main`
4. Нажмите **Run workflow**

## 📝 Обновление контента

Для обновления содержимого на GitHub Pages:

1. Внесите изменения в файлы в папке `/frontend`
2. Закоммитьте и запушьте в `main`:
   ```bash
   git add frontend/
   git commit -m "Update frontend"
   git push origin main
   ```
3. GitHub Actions автоматически задеплоит изменения

## 🔧 Локальная разработка

Для тестирования перед деплоем:

```bash
cd frontend
python3 -m http.server 8000
```

Откройте http://localhost:8000

## 📊 Мониторинг

- **Статус деплоя**: Badge в README (опционально)
- **Логи**: Вкладка Actions в GitHub
- **URL**: Настройки Pages покажут актуальный URL

## 🚀 Production готовность

Для production использования рекомендуется:
- Развернуть backend API на облачном сервере (AWS, GCP, Azure)
- Настроить HTTPS
- Использовать CDN для статики
- Добавить аналитику (Google Analytics)
